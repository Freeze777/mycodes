from sklearn import linear_model
import numpy as np


f,n = map(int,raw_input().split())

x_matrix = np.arange(n*(f+1), dtype='float')
x_matrix = x_matrix.reshape(n, f+1)

tmp=0
while tmp<n:
    x_matrix[tmp]=map(float,raw_input().split())
    print x_matrix
    #x_matrix.append(inp[0:(f-2)])
    #y_matrix.append(inp[f-1])
    tmp+=1
 
lin = linear_model.LinearRegression()
lin.fit(x_matrix, y_matrix)   

q=int(raw_input())

while q>0:
    x=map(float,raw_input().split())
    print round(lin.predict(x), 2)
    q-=1
