import java.util.*;

class TestClass {
	public static void main(String args[]) throws Exception {
		Scanner s = new Scanner(System.in);
		int l = s.nextInt();
		int m = s.nextInt();
		char[] str = s.next().toCharArray();
		
		if (m >= str.length) {
			System.out.println("");
			System.exit(0);
		}
		int start = 0;
		while (m > 0 && start<str.length) {
			char max = str[start];
			int max_loc = start;
			for (int i =0; i < m && (start+i)<str.length; i++) {
				if (str[start+i] > max) {
					max_loc = start+i;
					max = str[start+i];
				}
			}
			for (int i = start; i < max_loc; i++) {
				str[i] = '!';
			}
			m -= (max_loc - start);
			start = max_loc+1;
		}
		int j=str.length-1;
		while(m-->0){
			str[j]='!';
			j--;
		}
		for (int i = 0; i < str.length; i++) {
			if(str[j]!='!')
				System.out.print(str[i]);
		}
		
	}
}
