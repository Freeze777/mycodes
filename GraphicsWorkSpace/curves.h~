#include "display_utility.h"
float binomial_coff(float n,float k);
Point bezierHelper(Point p[],int size,double t);
void computeBezierCurvePoints(Point p[],int size,Point bezier[]);
void computeLagrangeCurvePoints(Point p[],int size,Point lagrange[]);
